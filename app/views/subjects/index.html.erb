<% @card_title = "Relação de " + @subjects.model_name.human(count: 2) %>
<% @btn_new = btn_new(new_subject_path) %>

<div class="table-responsive">
	<%= table_for(@subjects) do %>
		<% field_table(@subjects.model.human_attribute_name('name'))  { |f| f.name } %>
		<% btn_form %>
	<% end %>
</div>
